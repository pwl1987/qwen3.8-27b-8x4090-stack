# Rejected A/B arm: resident vision was 49 ms faster per maximum-pixel image,
# but left 235 MiB free and the engine failed after the 32K/cache workload.
# Keep for reproducibility; do not select as the active profile.
PORT=19622
HOST=0.0.0.0
SPEC=dflash2
DFLASH_TOKENS=7
PREFIX_CACHE=1
VISION=1
VISION_OFFLOAD=0
CTX=fast
MAX_SEQS=4
GPU_UTIL=0.93
REQ_METRICS=1
VLLM_NO_USAGE_STATS=1
DO_NOT_TRACK=1
