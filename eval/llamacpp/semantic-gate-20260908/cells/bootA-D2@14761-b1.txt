这份文档描述了一个编程代理在 Go 代码仓库中解决 GitHub 问题的过程。问题涉及 gqlgen 库在 v0.17.50 版本中，当客户端发送包含 `text/event-stream` 的 `Accept` 头时，HTTP POST 传输会拒绝处理非订阅请求，导致 "transport not supported" 错误。代理通过探索代码库、定位问题根源（`http_post.go` 中的 `Supports` 方法）、分析修复方案，最终修改了相关代码以允许 HTTP POST 传输处理带有 SSE Accept 头的非订阅请求。