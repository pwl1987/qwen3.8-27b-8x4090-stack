这份文档描述了一个编程代理在 Go 代码仓库中解决 GitHub 问题的过程。
问题核心是 gqlgen 库在 v0.17.50 版本中，HTTP POST 传输层错误地拒绝了包含 `text/event-stream` Accept 头的非订阅请求，导致大量客户端（如 urql）请求失败。
代理通过探索代码、复现问题、分析根因（POST 传输层的 `Supports` 方法过于严格），并实施修复（移除 POST 传输层对 SSE Accept 头的排斥逻辑），最终验证了修复的有效性并运行了相关测试。