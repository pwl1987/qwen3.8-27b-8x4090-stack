这份文档描述了一个编程代理的任务，旨在解决 gqlgen 库中因 HTTP POST 传输层错误拒绝包含 `text/event-stream` Accept 头的请求而导致的 "transport not supported" 错误。文档详细列出了从探索代码库、复现问题、分析根因到实施修复、验证测试和测试边缘情况的完整步骤。最终目标是修改非测试文件，使 HTTP POST 传输层不再因 Accept 头中包含 SSE 类型而拒绝普通查询和变更请求，从而兼容使用 urql 等客户端库的用户。