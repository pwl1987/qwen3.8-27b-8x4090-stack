这份文档描述了一个编程代理的任务，旨在解决一个 GitHub 问题。该问题涉及在更新到 v0.17.50 版本后，由于 HTTP POST 传输层对 `Accept` 头中 `text/event-stream` 的处理不当，导致所有 GraphQL 请求失败并返回 "transport not supported" 错误。文档详细列出了代理需要遵循的步骤，包括探索代码库、复现问题、分析根本原因、实施修复、验证解决方案、运行单元测试以及测试边缘情况，最终提交修复。